"""
Rattlesnake package — progressive split of claw_cli.py into modules.

Current modules:
  - providers: LLM provider adapters (Ollama, OpenRouter, OpenAI, Anthropic, DashScope)

Everything else still lives in the top-level claw_cli.py during the port.
"""
