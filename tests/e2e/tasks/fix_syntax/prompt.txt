There is a file named broken.py in the current directory that has a Python syntax error preventing it from running. Find and fix the bug so that `python broken.py` runs successfully and prints "Hello, world!".

Do not change the program's behavior — it should still greet "world" when run. Use read_file and edit_file.
